# Phase 1 Data Model: The About Page

No entities. The bio copy and photo are fixed, one-off page content, not a
repeating collection — unlike `002-content-stack`'s pieces, there is no
second "about page" that would ever need a second record. `spec.md`'s own
Key Entities section is omitted for the same reason, per the template's
instruction to remove sections that don't apply rather than mark them N/A.
