# Constitution — thisismaca.com

**This document has moved to [`.specify/memory/constitution.md`](.specify/memory/constitution.md).**

That is the location GitHub Spec Kit reads from, so the `/speckit-*` commands
see the real rules rather than a template. This file is a pointer only — do not
add content here, or the two copies will drift.
